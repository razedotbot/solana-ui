// Default split panel sizes for advanced view
export const DEFAULT_SPLIT_SIZES = [25, 75];

// Minimum split sizes
export const MIN_LEFT_COLUMN_SIZE = 0;
export const MIN_RIGHT_COLUMN_SIZE = 250;

// Actions column width
export const ACTIONS_COLUMN_WIDTH = 350;
