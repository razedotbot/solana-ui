{
  "permissions": {
    "allow": [
      "mcp__acp__Write",
      "mcp__acp__Bash",
      "mcp__acp__Edit",
      "Bash(npx tsc:*)",
      "Bash(npx eslint:*)",
      "Bash(dir:*)",
      "Bash(npm run build:*)",
      "Bash(python3 -c \"\npath = r''c:\\\\Users\\\\volpe\\\\Documents\\\\GitHub\\\\solana-ui\\\\src\\\\components\\\\TradingForm.tsx''\nwith open\\(path, ''r'', encoding=''utf-8''\\) as f:\n    content = f.read\\(\\)\n\n# Find and replace the orders content ternary + limit order conditionals\n# The structure is:\n# {activeMainTab === ''orders'' ? \\( <orders content> \\) : \\( <trading content> \\)}\n# We need to replace this with just the trading content \\(without limit order conditionals\\)\n\n# First, remove the orders ternary and keep only the trading content\n# Match from ''activeMainTab === \"\"orders\"\"'' section to the start of Trading Content\nold_orders_section = ''''''          {activeMainTab === \"\"orders\"\" ? \\(\n            /* Orders Content */\n            <div className=\"\"space-y-3\"\">''''''\n\n# Find the position\npos = content.find\\(old_orders_section\\)\nif pos == -1:\n    print\\(''ERROR: Could not find orders section''\\)\n    exit\\(1\\)\n\n# Find the matching ''\\) : \\('' that separates orders from trading\n# We need to find ''          \\) : \\('' followed by trading content\ntrading_marker = ''            /* Trading Content */''\ntrading_pos = content.find\\(trading_marker, pos\\)\nif trading_pos == -1:\n    print\\(''ERROR: Could not find trading content marker''\\)\n    exit\\(1\\)\n\n# Find the end of orders content \\(the line before trading marker\\)\n# Go backwards from trading_pos to find ''\\) : \\(''\nbefore_trading = content[pos:trading_pos]\nclose_paren_pos = before_trading.rfind\\(''\\) : \\(''\\)\nif close_paren_pos == -1:\n    print\\(''ERROR: Could not find \\) : \\(''\\)\n    exit\\(1\\)\n\n# Replace from orders section start to after the trading content marker\n# Remove orders content and the wrapping\n# The orders section looks like:\n# {activeMainTab === ''orders'' ? \\(\n#   ... orders content ...\n# \\) : \\(\n#   /* Trading Content */\n#   <>\n#     ... trading content ...\n#   </>\n# \\)}\n\n# We want to keep only the trading content without the fragment wrapper\n# Let''s find the exact boundaries\n\n# Find the start of the entire conditional\nentire_start = content.rfind\\(''{'', 0, pos\\)\n\n# Actually let me take a different, simpler approach\n# Replace the orders section and the trading content wrapper in steps\n\n# Step 1: Remove everything from ''{activeMainTab === \"\"orders\"\" ? \\('' to ''/* Trading Content */''\n# and the ''<>'' fragment wrapper  \nold = ''''''          {activeMainTab === \"\"orders\"\" ? \\(''''''\nnew = ''''''          {true ? \\(''''''\n\ncontent = content.replace\\(old, new, 1\\)\n\nwith open\\(path, ''w'', encoding=''utf-8''\\) as f:\n    f.write\\(content\\)\n\nprint\\(''Temporary replacement done''\\)\n\")",
      "Bash(python3:*)"
    ]
  }
}
