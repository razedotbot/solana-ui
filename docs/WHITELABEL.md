# Theme Customization

## Overview

This document previously described a theme customization tool. The theme customizer functionality has been removed from the application.

## Theme Customization

To customize the theme, directly edit the CSS variables in `src/styles/green.css` or create your own theme CSS file.

